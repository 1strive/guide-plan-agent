# 学习路线驱动的项目开发规划

> 基于你「有一定基础、快速迭代」的选择，本计划从阶段2（Prompt Engineering）开始重点投入，阶段1的核心概念通过代码实践快速覆盖。全周期约 **10 周**。

---

## 前置：阶段1 基础补强（第1周上半周，3~4天）

虽然你已有基础，但项目还需要补齐一些阶段1的**工程实践**，作为后续阶段的底座。

### Task 1.1：流式输出（Streaming）支持
- **学习目标**：理解推理参数（temperature、max_tokens）对输出的影响；掌握 SSE/流式 API 的设计。
- **项目实践**：在 `src/index.ts` 的 `/sessions/:id/chat` 接口中，增加 `POST /sessions/:id/chat/stream` 端点，使用 `text/event-stream` 返回 LLM 流式输出。
- **关联文件**：`src/index.ts`、`src/agent/llm.ts`
- **输出物**：可运行的流式对话接口；对比实验记录（不同 temperature 的输出差异笔记）。

### Task 1.2：Token 计数与成本控制
- **学习目标**：理解 Token、Tokenizer 概念；建立「按 token 计费」的工程意识。
- **项目实践**：在 `src/agent/llm.ts` 中添加请求/响应 token 估算逻辑（可先用字符数近似，再接入 tiktoken 或 OpenAI 返回的 usage 字段）；记录每次调用的 token 用量到日志。
- **关联文件**：`src/agent/llm.ts`、`src/config.ts`
- **输出物**：带用量统计的日志；单条请求成本计算笔记。

---

## 阶段2：Prompt Engineering（第1周下半周 ~ 第2周，约10天）

### Task 2.1：提示词版本化与结构化重构
- **学习目标**：掌握「系统提示 vs 用户消息」「角色-任务-约束-输出格式」的结构化写法；建立版本管理意识。
- **项目实践**：重构 `src/agent/prompts.ts`，从单字符串改为**提示词模板引擎**（支持变量插值），设计多版本提示词存储结构（如 `prompts/v1_base.ts`、`prompts/v2_cot.ts`）。
- **关联文件**：`src/agent/prompts.ts`
- **输出物**：结构化提示词模板；版本切换机制。

### Task 2.2：Few-shot 与 Chain-of-Thought（CoT）提示词
- **学习目标**：理解 Few-shot 示例选取原则；掌握 zero-shot CoT（「让我们一步步想」）的适用边界。
- **项目实践**：
  - 在提示词模板中增加 `examples` 字段，为旅游场景（如「推荐亲子游目的地」）注入 2~3 组 Few-shot 示例。
  - 增加 CoT 模式开关：在系统提示词末尾条件性附加「请逐步分析用户需求，再给出建议」。
- **关联文件**：`src/agent/prompts.ts`、`src/agent/llm.ts`
- **输出物**：两种提示词变体（标准版 vs CoT 版）。

### Task 2.3：Prompt 评测与迭代框架
- **学习目标**：建立「评测意识」——同一提示词用多组输入测试，记录失败案例并迭代。
- **项目实践**：
  - 在项目中新建 `src/eval/` 目录，创建测试集文件（10~30 条旅游问答用例）。
  - 写一个脚本 `scripts/eval-prompt.ts`，批量跑不同版本的提示词，输出准确率、失败案例列表。
  - 用 JSON 记录每次实验：版本号、参数、测试集、通过率、典型失败 case。
- **关联文件**：新增 `src/eval/testset.ts`、`scripts/eval-prompt.ts`
- **输出物**：可复用的 Prompt 评测流水线；至少完成 2 轮迭代记录。

---

## 阶段3：RAG 技术（第3周 ~ 第4周，约2周）

> 当前 `src/rag/` 为空，且系统提示词中提到了 `semantic_search_travel` 工具但 **尚未实现**，这是本阶段的核心战场。

### Task 3.1:文档切分与 Embedding 抽象
- **学习目标**:理解 chunk 大小、overlap 对检索质量的影响;掌握 embedding **多 provider 抽象**的工程价值(本任务真实工程教训:MiniMax 账号无 embedding 权限,逼出抽象层)。
- **项目实践**:
  - **切分**:从 `destinations` + `destination_features` 表生成 chunk(`src/rag/chunker.ts:buildAllChunks`)。
    - 策略:1 个 summary chunk + 每条 feature 各 1 chunk;**不做 overlap**(条目独立无上下文关联,overlap 反而引入噪声)。
    - **chunk text 自带前缀**:`"丽江的美景「玉龙雪山」:..."` 让 chunk 自我描述完整,不依赖外部上下文。
    - metadata 字段:`{ destinationId, destinationName, region, category, source }`,给 Chroma `where` 过滤和 Task 3.5 溯源用。
  - **Embedder 抽象**(`src/rag/embedder.ts`):统一接口 `generate(texts: string[]) → number[][]`(与 chromadb `EmbeddingFunction.generate` 对齐),工厂函数 `createEmbedder(config)` 按 `EMBEDDING_PROVIDER` 实例化。
  - **三档实现**:
    - `MinimaxEmbedder`:走 `embo-01`,需 token plan 支持(**当前 MiniMax 测试账号不支持,会 throw**——这是抽象层必须存在的根因)。
    - `OpenAIEmbedder`:走标准 `/embeddings` 协议(`text-embedding-3-small`,1536 维),需真 OpenAI key。
    - `DeterministicEmbedder`:**字符 2-gram + 3-gram → FNV-1a 哈希 → L2 归一化**(128 维,完全离线、零依赖、可复现)。**作为 dev/演示默认**——让 RAG 全链路在无任何外部 embedding 服务时也能跑通。
  - 注:本任务**不接入真模型**,只把抽象层和 deterministic fallback 落地;真模型在 Task 3.6 接入(Transformers.js + Xenova/bge-small-zh-v1.5 本地 ONNX)。
- **关联文件**:新增 `src/rag/types.ts`、`src/rag/chunker.ts`、`src/rag/embedder.ts`
- **关联八股**:
  - 03-RAG技术.md §2 切分策略 / §3.1 embedding 模型选型
  - 08-工程化实践.md §5 多 Provider 抽象(本任务是抽象的诞生)
- **输出物**:切分后的 chunk 数据集(18 条);3 个 Embedder 实现 + 工厂函数;一段"为什么 Embedder 必须抽象(MiniMax 无权限事件复盘)"的设计决策记录(写入代码注释或 `docs/03-开发笔记/note-03 §2`)。

### Task 3.2：向量存储与检索（Chroma）
- **学习目标**：掌握向量数据库 CRUD 与相似度检索；理解 cosine similarity；理解 HNSW 等向量索引相比"全表扫描算内积"的优势；掌握向量数据库的 docker 化部署与多语言客户端调用。
- **技术选型决定**：使用 **Chroma**（`chromadb/chroma` 官方镜像）作为向量存储。理由：① 接口贴合 LangChain/LlamaIndex 行业标准，便于后续阶段5 接入网关层 / 阶段6 写架构文档时讲选型；② docker 一键起,跟现有 docker-compose 加一个 service 即可,运维成本低;③ 内置 HNSW 索引,几十万向量内毫秒级查询，远好于"MySQL JSON 列 + SQL 算内积"的 MVP（学习项目目的地数据虽然小，但用真实生产架构练手价值更高）。
- **项目实践**：
  - **docker-compose 新增 `chroma` 服务**（镜像 `chromadb/chroma:latest`，暴露 8000 端口，挂卷持久化）。
  - **依赖与配置**：安装 `chromadb` JS client；`.env` 增加 `CHROMA_URL=http://127.0.0.1:8000` 与 `CHROMA_COLLECTION=destinations_v1`。
  - **接口抽象**：新建 `src/rag/vectorStore.ts` 定义 `VectorStore` 接口（`add / query / delete`），实现 `ChromaVectorStore`；其他 RAG 模块只依赖接口，给后续切 Milvus/Qdrant 留口。
  - **EmbeddingFunction 注入**:把 Task 3.1 定义的 `Embedder` 接口包装成 chromadb 要求的 `EmbeddingFunction`(两者 `generate(texts)` 签名一致,几行 wrapper)注入 collection;**具体走哪个 embedder 由 `EMBEDDING_PROVIDER` 决定**(deterministic / xenova / minimax / openai),vectorStore 层不感知具体实现。
  - **Collection 配置**：`metadata: { "hnsw:space": "cosine" }`，对齐主流 RAG 实践。
  - **核心函数**：实现 `searchSimilarChunks(query: string, topK: number, filter?)`，支持元数据过滤（如 `where: { category: "scenery" }`）。
- **关联文件**：新增 `src/rag/vectorStore.ts`；修改 `docker-compose.yml`、`.env.example`、`package.json`（加 `chromadb` 依赖）。
- **输出物**：可运行的语义检索接口；Top-K 结果示例；一段"为什么选 Chroma 而非 MySQL JSON 列或 Milvus/Pinecone"的技术选型说明（写入 `docs/02-实验记录/exp-03-vectorstore-selection.md`，作为面试 STAR 素材）。
- **关联八股**：
  - 03-RAG技术.md §向量存储 / §HNSW 索引
  - 08-工程化实践.md §部署架构（docker-compose 多服务编排）

### Task 3.3：实现 `semantic_search_travel` 工具
- **学习目标**：理解「检索 → 拼 prompt → 生成」的 RAG 完整流程。
- **项目实践**：
  - 在 `src/agent/tools.ts` 中新增 `semantic_search_travel` 工具定义与实现。
  - 该工具内部调用 RAG 检索，返回相似 chunk 文本列表。
  - 更新系统提示词，确保模糊需求（如「想看雪山又不想太累」）优先走语义检索。
- **关联文件**：`src/agent/tools.ts`、`src/agent/prompts.ts`、`src/rag/` 下各模块
- **输出物**：Agent 可正确调用语义检索并基于检索结果回答。

### Task 3.4：混合检索与重排序
- **学习目标**：理解「关键词 + 向量混合检索」的优势；了解 rerank 的作用与代价。
- **项目实践**：
  - 实现混合检索：将 `search_destinations`（关键词 LIKE）与 `semantic_search_travel`（向量检索）的结果用 **RRF（Reciprocal Rank Fusion）** 融合。
  - 增加轻量重排序：用 cross-encoder 或简单的「查询词与结果文本的重合度」作为第二遍排序。
  - 在评测脚本中对比「纯关键词」「纯语义」「混合」三种策略的准确率。
- **关联文件**：`src/rag/hybridSearch.ts`、`scripts/eval-prompt.ts`
- **输出物**：混合检索流水线；三种策略的对比实验数据。

### Task 3.5：生成与溯源
- **学习目标**：减少幻觉的 prompt 技巧；让模型引用来源。
- **项目实践**：
  - 在系统提示词中增加约束：「回答时请标注信息来源（目的地名称及类型），如果不确定请说明」。
  - 在 API 响应中增加 `sources` 字段，返回引用的 chunk 列表。
- **关联文件**：`src/agent/prompts.ts`、`src/index.ts`
- **输出物**：带溯源信息的回答响应。

---

## 整合阶段:LangGraph 主线切换 + 会话续流(承上启下)

> **本阶段不属于阶段3 也不属于阶段4,是横跨二者的"框架整合"**。背景:阶段3 完成 RAG 主体后,真实使用反馈出会话续流的强需求(切走会话不应丢任务),手写实现成本相当于 Task 4.5 完整复杂度;借力 LangGraph 的 `thread_id` + `Checkpointer`,接框架的工程成本远低于手写。所以决定**先切到 LangGraph 主线(整合-1)+ 用 LangGraph 现成能力一次性做 Run-as-Resource 完整版(整合-2,吸收原 Task 4.5)**,然后再回到阶段3 完成 Task 3.6/3.7,再进阶段4(精简为 4.1/4.2/4.3/4.4)。
>
> **执行顺序**:Task 3.1~3.5(✅ 已完成)→ **Task 整合-1(✅ 已完成)→ Task 整合-2** → Task 3.6 → Task 3.7 → 阶段4。
>
> **取消/合并的旧任务**:① Task 4.6"LangGraph 对照实现"取消(整合-1 已切主线);② Task 4.5"Run 持久化完整版"整体并入整合-2。理由见关键设计决策 #6 升级版。

### Task 整合-1:切换主 Agent 为 LangGraph(主线替换)
- **学习目标**:掌握 `createReactAgent` 的实际使用 + StateGraph + Checkpointer 的"持久状态"心智;学会 LangGraph 事件流与 AG-UI 协议的 **adapter** 写法;真正理解"为什么大厂选 LangGraph 不只是为了 `createReactAgent`,而是为了 `thread_id` + `Checkpointer` 给状态管理铺的路"。
- **背景说明**:原 Task 4.6 设想"手写主线 + LangGraph 对照"。但整合阶段要做"会话续流"(Task 整合-2),**LangGraph 的 thread_id + Checkpointer 让续流实现成本远低于手写**——所以决定**在整合阶段先切到 LangGraph 主线**,Task 4.6 取消。这是真实工程常见决策:发现某能力靠手写做要 2 周、接框架只要 2 天,就该接框架。
- **技术选型决定**:
  - LangGraph 核心:`@langchain/langgraph` 的 `createReactAgent`
  - Checkpointer:开发用 `MemorySaver`(进程内),整合-2 升级到 `SqliteSaver` 或自研 `MySQLSaver`
  - LLM 适配:`@langchain/openai`(MiniMax 走 OpenAI 兼容协议)
  - **保留 AG-UI 事件协议**,写 adapter 翻译 LangGraph 的 `streamEvents` v2 输出
- **项目实践**:
  - **依赖**:`npm i @langchain/core @langchain/langgraph @langchain/openai`
  - **核心实现**(新增 `src/agent/langgraph-agent.ts`,目标 < 200 行):
    - 用 `createReactAgent` 注册阶段3 的 3 个工具(`search_destinations` / `get_destination_detail` / `semantic_search_travel`)
    - 工具用 zod schema 定义(LangGraph 标准),复用现有 `runTool` 的实现逻辑
    - 用 `MemorySaver` 作 Checkpointer,`thread_id = sessionId`
    - 流式输出走 `graph.streamEvents(input, { version: 'v2', configurable: { thread_id }})`
  - **事件 Adapter**(新增 `src/agent/langgraphToAgUi.ts`):
    - 把 LangGraph 的 `on_chat_model_stream / on_tool_start / on_tool_end / on_chain_end` 翻译成项目原生 AG-UI 事件(RUN_STARTED / TEXT_MESSAGE_* / TOOL_CALL_* / RUN_FINISHED)
    - 保持前端 SSE 协议**完全不变**(前端无感切换 backend)
    - 保持 `RUN_FINISHED.sources` 字段(从工具结果聚合,逻辑跟手写时一致)
    - 保持 `[ASK_USER]` 协议检测(在 adapter 层做,**不立即升级到 LangGraph 原生 `interrupt()`**,留给整合-2 评估)
  - **handler 切换**:`src/index.ts` 的 `POST /sessions/:id/stream` 改调 `runLangGraphAgent`;`runAgentStream` 退役但**代码保留**(打 `@deprecated` 注释 + 简短说明"replaced by langgraph-agent.ts at Task 整合-1")
  - **prompts 不变**:LangGraph 同样接受 system prompt + Few-shot prepend messages,`v1_base` 全套约束直接复用
  - **tools.ts 不变**:工具实现复用,只是注册到 LangGraph 而非自己手写循环调用
  - **sanitize 不变**:入口 / 出口 detectInjection / wrapUntrusted 仍在 handler 层做,跟 backend 解耦
- **关联文件**:
  - 新增 `src/agent/langgraph-agent.ts`、`src/agent/langgraphToAgUi.ts`
  - 修改 `src/index.ts`(handler 切换)、`src/agent/llm.ts`(`runAgentStream` 加 deprecated 注释)、`package.json`(加 3 个 LangChain 依赖)
- **关联八股**:
  - 02-核心框架.md §6 LangGraph 状态机 / Q9~Q11 何时选 LangGraph
  - 02-核心框架.md ReAct 循环(手写 vs `createReactAgent` 的对比)
- **关键决策与边界**:
  - ✅ **主线切换,不保留双 backend**:原 Task 4.6 "并存对照"取消
  - ✅ **保留 AG-UI 协议**:adapter 层吸收 LangGraph 事件差异,前端 0 改动、评测脚本 0 改动
  - ✅ **删除手写代码**:`runAgentStream` / `postChatStream` 等不保留,`src/agent/llm.ts` 瘦身为只导出 `ChatMessage` / `ResumeItem` / `TokenUsage` 类型;STAR 故事("手写 vs LangGraph 对比")写在 `docs/03-开发笔记/note-04` 而不是靠源码留档
  - ✅ **MemorySaver 起步**:Checkpointer 用最简单的进程内实现,Task 4.5 完整版再升级到持久化存储
  - ❌ **不引入 LangChain 全家桶**:只装 `@langchain/core` + `@langchain/langgraph` + `@langchain/openai` 三个最小包
  - ❌ **不立即用 LangGraph 原生 `interrupt()`**:`[ASK_USER]` 字符串协议先保留,整合-2 评估是否升级
- **输出物**:
  - ① LangGraph 主路径可演示对话(同测试集 + 前端 + 评测脚本零侵入切换通过)
  - ② AG-UI adapter 层(LangGraph 事件 → 项目原生事件)
  - ③ 一段"为什么阶段3 末就切主线,而不是 Task 4.6 时再做对照"的决策记录(写入 `docs/03-开发笔记/note-04` 或本规划)

### Task 整合-2:Run-as-Resource(完整版,吸收原 Task 4.5 全部能力)
- **学习目标**:理解"HTTP 请求 ≠ Agent Run"的解耦原则;掌握长任务系统的事件回放、订阅恢复、三态 abort(被动断开 / 主动取消 / 系统关停)的工程实现;建立"Run-as-Resource"心智模型(对标 LangGraph runs / OpenAI Assistants threads);借力 LangGraph `thread_id` + 持久化 Checkpointer 实战。
- **背景说明**:原计划"轻量版整合-2 + 完整版 Task 4.5"两步走;决策升级为**一次性做完整版**——理由:轻量版基础设施(runRegistry、DB 表、handler 改造)和完整版重叠 70%,拆两步会产生重复工作 + 接口反复调整。整合-1 切到 LangGraph 后,Checkpointer + thread_id 都是现成的,正好一次性把"Run-as-Resource"做透。**Task 4.5 整体被吸收,阶段4 不再做**(见关键设计决策 #6 升级版)。
- **依赖**:Task 整合-1(LangGraph 主线必须先就绪)。
- **覆盖 5 大需求集**:
  - 【**A. 切走不停 + 续订**】(原"轻量版"4 条需求)
    1. 前端:切换会话后旧会话 SSE 必须停(reader.cancel + AbortController per session)
    2. `GET /messages` 加 `status` 字段;running 时前端续流带 `lastEventSeq`
    3. 客户端断开 ≠ abort Run;Run 在内存继续跑 + 持续写库;完成后 status 转 'completed'
    4. 续流时对比 seq,补发缺失事件
  - 【**B. 主动取消**】(原 Task 4.5)
    5. 前端显式"停止"按钮 → `POST /sessions/:id/runs/:runId/cancel`(202 Accepted,幂等)
    6. 三态 AbortController 模型:per-subscriber(req close → 仅 unsubscribe)vs per-run(cancel/SIGTERM/超时 → 真 abort)**两个 controller 无级联**
  - 【**C. 事件级 seq 回放**】(原 Task 4.5)
    7. 每条 AG-UI 事件写 `agent_run_events(run_id, seq, event_json)`;同事件序列回放结果可重复且与首次完全一致
    8. `GET /sessions/:id/runs/:runId/stream?after_seq=N` 续订接口:先回放 N 之后的事件,再接实时流
  - 【**D. 完整状态机 + 多订阅者**】(原 Task 4.5)
    9. 状态机:`pending → running → {completed | interrupted | cancelling → cancelled | failed}`
    10. 同一 Run 支持多个订阅者并发续订(多 tab 打开同会话)
  - 【**E. 持久化 Checkpointer + 进程重启降级**】(原 Task 4.5)
    11. LangGraph Checkpointer 从 MemorySaver 升级到 `@langchain/langgraph-checkpoint-sqlite` 的 SqliteSaver(或自研 MySQLSaver,跟 chat_sessions 同库)
    12. 进程重启:扫描 `agent_runs.status='running'` 全标 `failed`;前端续订时显式提示"由于服务重启,本次任务已中断"(Redis Pub/Sub 跨进程方案推迟到 Task 5.4 容器化)
- **项目实践**:
  - **DB 改动**(新增 `src/db/migrations/003_agent_runs_events.sql`):
    - `chat_sessions` 加 `status ENUM('running','end') NOT NULL DEFAULT 'end'`(简化版,供 /messages 快查)
    - **新表 `agent_runs(run_id PK, session_id, status, started_at, finished_at, last_event_seq, total_tokens)`**,完整状态机
    - **新表 `agent_run_events(run_id, seq PK part, event_json, created_at)`**,事件流水
    - 启动清理:`UPDATE agent_runs SET status='failed' WHERE status IN ('pending','running','cancelling')`
  - **DB 仓**:扩展 `src/db/chatRepo.ts`(status 字段) + 新增 `src/db/runRepo.ts`(agent_runs / agent_run_events CRUD + seq 自增 + 回放查询)
  - **核心新模块 `src/agent/runManager.ts`**(进程内 Run 注册表):
    - `start(sessionId, runInput) → runId` — 创建 Run,启动 LangGraph,挂上 per-run AbortController,事件实时写 agent_run_events
    - `subscribe(runId, onEvent) → unsubscribe` — 加订阅者,可多个;断开只 unsubscribe 不 abort
    - `cancel(runId)` — 触发 per-run AbortController + status='cancelling'→'cancelled'
    - `getActive(sessionId)` — 查最近未完成 Run
    - 三态 abort 模型:per-subscriber Controller + per-run Controller,**互不级联**
  - **Checkpointer 升级**:换 LangGraph 的 `@langchain/langgraph-checkpoint-sqlite` SqliteSaver(或自研 MySQLSaver 跟 chat_sessions 同库,如果想统一);进程重启后 LangGraph 自身的 thread 状态也能恢复
  - **handler 改造**(`src/index.ts`):
    - `POST /sessions/:id/stream`:改为 `runManager.start + subscribe`(不再阻塞读 LangGraph 流自己 yield)
    - `GET /sessions/:id/runs/active` — 返回最近未完成 Run 或 null
    - `GET /sessions/:id/runs/:runId/stream?after_seq=N` — 续订:`runRepo.queryEvents(runId, after_seq)` 回放 + `runManager.subscribe(runId)` 接实时
    - `POST /sessions/:id/runs/:runId/cancel` — `runManager.cancel(runId)`,202 + 幂等
    - `GET /sessions/:id/messages` 加 status 字段(从 chat_sessions 直查)
    - `req.raw 'close'` 钩子:改为 `runManager.unsubscribe(runId, subscriberId)`,**不再 ctl.abort()**
  - **Adapter 改造**(`src/agent/langgraphToAgUi.ts`):
    - 每 yield 一个 AG-UI 事件,同步调 `runRepo.appendEvent(runId, seq++, event)`
    - LangGraph 的 `on_chat_model_stream` 聚合到 assistant 消息时增量 update `chat_messages`(避免每 token 一行)
    - `on_chain_end` 时:`runManager.complete(runId)` + 更新 chat_sessions.status='end' + agent_runs.status='completed'
  - **前端配套**(`web/src/api.ts` + `web/src/App.tsx`):
    - `switchSession` 持 AbortController,切前 `abort()` + reader.cancel
    - 打开会话 → `GET /runs/active` → 有活跃 Run 则用 `?after_seq=last_seq` 续订;无则走历史接口
    - 加显式「停止」按钮 → `POST /cancel`
  - **架构文档同步**:`docs/04-架构文档/agent-架构.md` §3.3(中断处理改写)、§6 局限表移除"切走会话=任务终止"等多条、§1.1 分层图加 runManager
- **关联文件**:
  - 新增 `src/db/migrations/003_agent_runs_events.sql`、`src/db/runRepo.ts`、`src/agent/runManager.ts`
  - 修改 `src/db/chatRepo.ts`、`src/agent/langgraph-agent.ts`、`src/agent/langgraphToAgUi.ts`、`src/index.ts`(多个新路由 + 现有路由改造)、`web/src/api.ts`、`web/src/App.tsx`
  - 依赖:`npm i @langchain/langgraph-checkpoint-sqlite`(或不装,自研 MySQLSaver 跟 chat_sessions 同库)
- **关联八股**:
  - 02-核心框架.md ReAct 循环 / Run-as-Resource 模型
  - 05-记忆系统.md §短期记忆(agent_run_events = episodic memory 的工程形态)
  - 08-工程化实践.md §1 容错(细粒度 abort)/ §3 全链路可观测(事件日志即审计源)
- **关键决策与边界**:
  - ✅ **完整三态 abort 模型**:per-subscriber 仅 unsubscribe,per-run 才真 abort
  - ✅ **事件级 seq 回放可重复**:同事件序列回放结果与首次完全一致
  - ✅ **持久化 Checkpointer**:进程重启后 LangGraph thread 状态可恢复(但运行中 Run 仍标 failed,见下)
  - ✅ **多订阅者并发续订**:同 Run 多 subscriber 共享事件流
  - ⚠️ **进程重启的 Run 不自动续跑**:扫描标 failed,前端显式提示"已中断";"真正续跑"需要 LangGraph 重建 stream + 复用 checkpoint,复杂度收益不匹配,推迟
  - ❌ **跨进程方案(Redis Pub/Sub)推迟到 Task 5.4 容器化阶段**
- **输出物**:
  - ① 可演示完整链路:切走 → 主动停 → 回来 → 重启等各场景下的行为正确
  - ② 12 个具体需求逐条对应的验证步骤
  - ③ 一段能讲清楚"为什么 HTTP 请求和 Run 必须解耦 + 三态 abort 必要性"的技术决策记录(对应面试 STAR 故事)
  - ④ 事件回放 demo:同 runId 多次调 stream?after_seq=0,事件序列字节级一致

---

## 阶段3 续:本地 embedding + 联网搜索

> 整合阶段做完后回到阶段3 收尾。这两个 Task 跟 RAG 主线密切相关,但因整合阶段优先级更高,故拆到整合阶段之后。完成后即进入阶段4。

### Task 3.6:本地 Embedding 服务(Transformers.js + ONNX)
- **学习目标**:理解 ONNX 运行时在 JS 生态中的部署形态(进程内、零服务、零端口);掌握"换 provider 不动业务代码"的抽象有效性验证;具备把任意 HF 模型(只要有 Xenova/ONNX 版本)接入项目的能力。
- **背景说明**:Task 3.1 落地时 MiniMax 账号无 embedding 权限,临时走 `deterministic` 字符 n-gram 算法,流程能跑但无真语义(`exp-04` 实测 q2 "辣"无法匹配"火锅")。本任务把 embedding 切到真模型,只做**功能落地**,不做 deterministic/本地/云端三档对照实验(降低范围,聚焦"能跑")。
- **技术选型决定**:使用 **Transformers.js**(`@xenova/transformers`) + **Xenova/bge-small-zh-v1.5**(ONNX 量化版,512 维,~95MB)。理由:① **零外部服务**——直接在 Node 进程内跑 ONNX runtime,不需要额外 docker 容器、不占端口、不需要管 Ollama/TEI 这类后台进程;② **中文优化**——bge-small-zh-v1.5 是 BAAI 的中文专项轻量模型,对应我们旅游推荐场景的中文 query;③ **Xenova 仓库 ONNX 量化版**模型大小压缩到 ~95MB,Node 启动可接受;④ **完全离线**——首次运行自动下载到 `~/.cache/huggingface`,后续无网络依赖;⑤ **生态对齐**——`@xenova/transformers` 是 LangChain.js / LlamaIndex.ts 默认的本地 embedding 选项,面试讲选型有共识。
- **项目实践**:
  - **依赖**:`npm i @xenova/transformers`(自带 `onnxruntime-node`,无需额外装)。
  - **新增 `XenovaEmbedder`**(`src/rag/embedder.ts`):
    - 用 `pipeline('feature-extraction', modelId)` 加载模型,**lazy 单例**(首次 generate 时初始化,避免冷启动拖慢主流程)
    - 调用:`pipe(texts, { pooling: 'mean', normalize: true })` → 内部 mean pooling + L2 normalize,直接得到归一化向量
    - 维度查表:`Xenova/bge-small-zh-v1.5` = 512 维(写到 `MODEL_DIM_MAP` 便于切换其他 Xenova 模型如 gte-small 384 / bge-m3 1024 时一行扩展)
  - **`.env` 增加配置项**:
    - `EMBEDDING_PROVIDER=xenova`(切换入口)
    - `XENOVA_MODEL=Xenova/bge-small-zh-v1.5`(模型 id,可换其他 Xenova/* 模型)
  - **`config.ts`** zod 化:`EMBEDDING_PROVIDER` 枚举加 `'xenova'`;新增 `XENOVA_MODEL` 字符串字段
  - **`createEmbedder` 工厂**新增 `case 'xenova'` 分支
  - **重灌向量**:bge-small-zh-v1.5 是 512 维,跟当前 128 维 deterministic 不匹配,必须 `npm run index` 全量重灌(Chroma 会自动 drop 旧 collection 重建)
  - **smoke 验证**:跑 `npm run eval -- --case sem-01` 看模型能否正确选 `semantic_search_travel`(本任务不做完整对照,只验证流程通)
- **关联文件**:`src/rag/embedder.ts`、`src/config.ts`、`.env`、`.env.example`、`package.json`(加 `@xenova/transformers` 依赖)
- **关联八股**:
  - 03-RAG技术.md §3.1 embedding 模型选型(本地 ONNX 路径)
  - 08-工程化实践.md §5 多 Provider 抽象(本任务是抽象有效性的直接验证 —— deterministic → xenova 业务代码 0 改动)
- **关键决策与边界**:
  - ✅ **进程内 ONNX 而非独立服务**:跟 Ollama/TEI 的 docker 方案不同,Transformers.js 把模型加载到 Node 进程,适合学习项目"少一个运维点"的偏好
  - ✅ **lazy 单例初始化**:`getEmbedder()` 不会在启动时立即加载模型;`npm run dev` 启动仍然秒级;首次 RAG 调用慢 2~5s,后续推理快(本机 CPU 几十 ms)
  - ❌ **不做对照实验**:不写 `exp-05-embedding-comparison.md`,不跑 deterministic vs xenova 召回率对比表(聚焦"功能落地")
  - ⚠️ **内存代价**:Node 进程内存增加约 100~200MB(模型 + ONNX runtime),生产部署需注意
- **输出物**:可一键 `EMBEDDING_PROVIDER=xenova && npm run index` 切换到本地 ONNX embedding 的能力;一段"为什么选 Transformers.js 而不是 Ollama 独立服务"的技术选型说明(写入本规划或代码注释)。

### Task 3.7:联网搜索工具(Tavily)
- **学习目标**:理解 Agent "外部世界感知" 的工程模式;掌握第三方 API 工具的接入、缓存、配额管理;从"封闭数据库 demo"升级到"开放数据源 Agent",这是项目走出"玩具感"的关键一步。
- **背景说明**:当前知识库只有 3 个目的地(`scripts/seed.ts` 硬编码),Agent 永远只能推荐成都/丽江/哈尔滨。本任务接入联网搜索,让 Agent 能回答任意目的地的实时问题(如"东京 12 月有什么活动")。**注意**:这不是删除 SQL 工具,而是**加一条新工具路径**——SQL/RAG/Web 三路共存,模型按场景选;后续阶段4 Task 4.4 会把它们统一封装为 MCP Server。
- **技术选型决定**:使用 **Tavily Search API**(`tavily-js`)。理由:① 专为 LLM 设计,返回的就是清洁文本片段(不像 Google/Bing 返回 HTML 要再 parse);② 免费额度 1000 次/月,学习项目够;③ 内置 `topic` / `time_range` / `include_answer` 等 LLM 友好参数;④ 业界主流(LangChain、CrewAI 默认集成),面试讲选型时有共识。
- **项目实践**:
  - **依赖与配置**:`npm i @tavily/core`;`.env` 增加 `TAVILY_API_KEY`;`config.ts` zod 化。
  - **新增 `web_search` 工具**(`src/agent/tools.ts`):参数 `{ query, max_results?, search_depth? }`,返回带 url + title + snippet 的结构化结果。
  - **缓存层**:`src/agent/webSearchCache.ts` 用 MySQL 表 `web_search_cache(query_hash, response_json, created_at)`,TTL 默认 24h——同问题不重复烧 Tavily 额度。
  - **Source 类型扩展**:`Source` 加 `type: 'destination' | 'url'`,URL 类型的 source 含 `{ url, title, snippet }`,前端可渲染为可点击链接。
  - **system prompt 更新**(`v1_base.toolUsageRules`):新增"当问题涉及实时信息(开园时间、活动、价格、当前天气等)或数据库不覆盖的目的地时,调用 `web_search`"。
  - **注入防御扩展**:Web 检索结果是间接注入高危区(网页可能含 prompt injection),返回的每条 snippet 都走 `detectInjection` + `wrapUntrusted`(本机制已在 Task 3.3 / `note-02 §5.6` 备好,这里直接复用)。
  - **测试集扩充**:`testset.ts` 新增 5 条 `web-*` case,如「北京 2026 年春节有什么活动」「东京 12 月推荐景点」(seed 数据库不覆盖)。
- **关联文件**:`src/agent/tools.ts`、`src/agent/prompts/v1_base.ts`、`src/agent/sanitize.ts`(复用)、新增 `src/agent/webSearchCache.ts`、`src/db/migrations/004_web_cache.sql`、`src/eval/testset.ts`、`src/agent/ag-ui.ts`(扩 Source)
- **关联八股**:
  - 04-工具调用.md §3 外部 API 工具 / §5 缓存与配额
  - 08-工程化实践.md §4 间接注入防御(Web 是高危源)
- **输出物**:可演示的"问任意目的地"能力(对照之前只能答 3 个目的地的尴尬);Web 注入防御实测;Tavily 调用成本日志。
- **后续衔接**:阶段4 Task 4.4 会把 `web_search` / `semantic_search_travel` / `get_destination_detail` 统一封装为 MCP Server,并组合成 Agent Skills。

---

## 阶段4：Agent 核心（第5周 ~ 第6周中，约2.5周）

> 本阶段聚焦 ReAct 增强 + Plan 模式 + 记忆分层 + MCP/Skills 四件事。**原 Task 4.5(Run-as-Resource 完整版)已在整合阶段完成,Task 4.6(LangGraph 对照)已取消**——见整合阶段引言。Task 4.3 记忆分层依赖 整合-2 已建好的 agent_runs / agent_run_events 持久层(订阅 RUN_FINISHED 事件做摘要)。建议执行顺序:4.1 → 4.2 → 4.3 → 4.4。

### Task 4.1：增强 ReAct 循环与可观测性
- **学习目标**：深入理解 ReAct（推理 + 行动交错）的每一步；掌握 Agent 调试技巧。
- **项目实践**：
  - 在 `src/agent/llm.ts` 的 `runAgentWithTools` 中，增加**思考过程记录**：每轮将模型的 reasoning 文本（若有）存入独立字段。
  - 增加 `X-Agent-Trace-Id` 头部返回，将每轮的工具调用、参数、结果、耗时记录到日志，便于事后复盘。
- **关联文件**：`src/agent/llm.ts`
- **输出物**：可查看的 Agent 执行 trace；至少 3 个复杂查询的调试记录。

### Task 4.2：Plan-and-Execute 模式对比实现
- **学习目标**：掌握 Plan-and-Execute 与 ReAct 的差异；能在项目中切换模式。
- **项目实践**：
  - 新增 `src/agent/planner.ts`：实现「先规划再执行」模式——先用 LLM 生成任务计划（JSON 格式步骤列表），再按步骤执行。
  - 在 `src/index.ts` 中通过请求参数 `?mode=react|plan` 支持两种模式切换。
  - 对比实验：同一复杂问题（如「帮我规划一个7天云南游，包含美食和景点」）在两种模式下的步骤数、耗时、完成率。
- **关联文件**：新增 `src/agent/planner.ts`、`src/index.ts`
- **输出物**：可切换的双模式 Agent；对比实验报告。

### Task 4.3：记忆分层（长期记忆 + 用户画像）
- **学习目标**：掌握对话截断、摘要、用户画像的实现思路；理解「事件流水（episodic）」与「语义摘要（semantic）」两种记忆形态的差异。
- **依赖**：Task 4.5 提供的 Run 完成事件作为摘要触发时机；与 4.5 共用 `agent_runs` / `agent_run_events` 持久层。
- **项目实践**：
  - **对话摘要**：订阅 `RUN_FINISHED` 事件，当历史消息超过 `CHAT_HISTORY_LIMIT` 时，用 LLM 生成「用户偏好摘要」（如「用户喜欢自然风光、不喜欢太累的行程」），存储到 `chat_sessions` 表的 `summary` 字段。
  - **用户画像提取**：在 `src/agent/` 中增加 `memory.ts`，每次对话后更新用户偏好向量（关键词列表）。
  - 在构造 prompt 时，将摘要作为 system prompt 的一部分注入。
  - **记忆形态对照笔记**：写一段说明，把本任务的"语义摘要"和 4.5 的"事件流水"对应到八股 05 §短期/长期记忆模型，作为面试故事点。
- **关联文件**：新增 `src/agent/memory.ts`、`src/db/chatRepo.ts`
- **输出物**：跨会话的记忆保持效果演示；摘要生成示例；记忆形态对照笔记。

### Task 4.4：MCP（Model Context Protocol）接入 + Agent Skills
- **学习目标**：理解 MCP 与「普通 function calling」在架构上的区别;掌握 **Agent Skills** 模型(把若干 tools + prompt 打包成可复用能力,对标 Anthropic Agent Skills / Claude Code skills);能讲清楚"工具(Tool)→ 工具集(Server)→ 技能(Skill)"三层抽象的演进路径。
- **背景说明**:阶段3 加完 `web_search` / `semantic_search_travel` / `get_destination_detail` 后,Agent 有 3 个工具但散在本地代码。本任务做两件事:① 把它们都搬到 MCP Server,跨进程标准化;② 用 Skill 概念把它们组合成"旅游推荐"等可复用能力包,模型选用 Skill 而非散选 tool,大幅降低决策复杂度。
- **项目实践**:
  - **MCP Server 化**(新增 `src/mcp/servers/`):
    - `destinationServer.ts`:封装 `get_destination_detail`(走 stdio 传输)
    - `webSearchServer.ts`:封装 `web_search`(走 stdio 传输)
    - `ragServer.ts`:封装 `semantic_search_travel`(走 stdio 传输)
    - 三个 server 共用 schema 校验、错误处理、token 计数中间件
  - **MCP Client**(新增 `src/mcp/client.ts`):Agent 侧实现,启动时连接所有配置的 MCP server,把它们暴露的 tools 注册到 LLM 的 function calling schema
  - **跑通外部 MCP**:接入官方 filesystem MCP server(`@modelcontextprotocol/server-filesystem`),让 Agent 能读写本地文件,验证 MCP 跨实现兼容性
  - **Agent Skills 落地**(新增 `src/skills/`):
    - 每个 Skill 是一个文件夹,含 `SKILL.md`(描述何时触发 + 使用的 tools + system prompt 片段)+ 可选 `examples/` Few-shot
    - 至少落地 2 个 Skill:
      - `travel-recommend/`:组合 `web_search` + `semantic_search_travel` + `get_destination_detail`,推荐旅游目的地
      - `local-research/`:组合 filesystem + web_search,做本地文档调研(展示 Skill 的非旅游适用性)
    - **Skill Loader**(`src/skills/loader.ts`):启动时扫描 `src/skills/` 目录,把每个 SKILL.md 注入到 system prompt 的可选段落,模型根据用户意图决定加载哪个 Skill
    - 参考实现:Anthropic Agent Skills 规范(https://docs.claude.com/en/docs/agents-and-tools/agent-skills)
  - **prompts 更新**:`v1_base` 增加 `skillRegistry` section,枚举可用 Skill 名称 + 一句话触发条件
  - **评测扩展**:`testset.ts` 加 `skill-*` case,验证模型能选对 Skill;考察"启用 vs 关闭 Skill"时同 case 的决策质量
- **关联文件**:新增 `src/mcp/(servers|client.ts)`、`src/skills/{travel-recommend,local-research}/`、`src/skills/loader.ts`、`src/agent/prompts/v1_base.ts`(skillRegistry section)
- **关联八股**:
  - 04-工具调用.md §6 MCP 协议 / §7 Agent Skills 抽象
  - 02-核心框架.md §Tool ecosystems(工具 → MCP → Skill 三层抽象)
- **输出物**:
  - ① 3 个自研 MCP Server + 1 个外部 MCP Server 接入演示
  - ② 2 个可工作的 Skill;开/关 Skill 的决策质量对比数据
  - ③ 一段"为什么 Skill 比直接给一堆 tool 更好"的技术决策记录(对应面试 STAR 故事)

> **Task 4.5 已被整合阶段吸收**:原"Run 持久化、续流与三态中断(完整版)"的全部 5 个能力(主动取消 + 三态 abort + 事件 seq 回放 + 完整状态机 + 多订阅者 + Checkpointer 持久化)已合并到 **Task 整合-2**。完整内容见整合阶段段落。决策理由:轻量版 + 完整版两步走会产生 70% 重复工作 + 接口反复调整,一次性做完整版更省事;关键设计决策 #6 升级版有详细说明。
>
> **Task 4.6 已取消**:原"LangGraph 对照实现 + 双 backend 并存",因整合-1 已切 LangGraph 主线。手写 `runAgentStream` 代码保留在 `src/agent/llm.ts` 作历史参考(`@deprecated`),"手写 vs LangGraph 对比" STAR 故事在整合-1 输出物里讲。

---

## 阶段5：多智能体与工程化（第7周 ~ 第8周，约2周）

### Task 5.1：多 Agent 协作架构
- **学习目标**：理解 Supervisor 模式、角色分工、结构化消息传递。
- **项目实践**：
  - 新增 `src/agents/` 目录（注意复数，与单 agent 区分）：
    - `supervisor.ts`：意图识别 + 任务路由，不直接生成回答。
    - `ragAgent.ts`：专注于检索和答案生成（复用阶段3的 RAG 能力）。
    - `toolAgent.ts`：专注于外部工具调用（数据库查询、MCP 工具）。
  - Agent 间通过 JSON Schema 消息传递，Supervisor 负责整合结果。
  - 新增路由接口 `POST /orchestrate`，演示多 Agent 协作处理复杂问题。
- **关联文件**：新增 `src/agents/supervisor.ts`、`src/agents/ragAgent.ts`、`src/agents/toolAgent.ts`
- **输出物**：多 Agent 协作 Demo；单 Agent vs 多 Agent 的准确率/延迟对比。

### Task 5.2：模型路由与降级策略
- **学习目标**：掌握按任务类型、成本、延迟选择模型的策略。
- **项目实践**：
  - 在 `src/config.ts` 中配置多模型：`MODEL_LIGHT`（轻量模型，处理简单意图识别）、`MODEL_HEAVY`（强模型，处理复杂生成）。
  - 新增 `src/agent/router.ts`：根据查询复杂度（字数、关键词、历史模式）选择模型。
  - 实现降级：当强模型超时或报错时，自动切换到轻量模型 + 简化提示词。
- **关联文件**：新增 `src/agent/router.ts`、`src/config.ts`
- **输出物**：模型路由决策日志；降级触发记录。

### Task 5.3：容错、重试与可观测性
- **学习目标**：建立超时、重试、退避、幂等的工程意识。
- **项目实践**：
  - 在 `src/agent/llm.ts` 的 `postChat` 中增加：指数退避重试（3次）、超时控制（ configurable ）。
  - 工具调用失败时，不再直接返回 error JSON，而是尝试替代策略（如换一个检索方式）。
  - 增强日志：每个请求带 `trace_id`，记录总耗时、每步耗时、token 用量、模型名称、是否降级。
  - 新增 `GET /metrics` 接口，返回近 N 次请求的统计（平均延迟、错误率、token 消耗）。
- **关联文件**：`src/agent/llm.ts`、`src/index.ts`
- **输出物**：压力测试下的容错表现；metrics 接口返回的统计数据。

### Task 5.4：容器化与部署完善
- **学习目标**：掌握 Dockerfile、docker-compose、环境变量管理、简单 CI/CD。
- **项目实践**：
  - 优化现有 `docker-compose.yml`，增加应用服务容器（当前只有 MySQL）。
  - 编写 Dockerfile，支持多阶段构建（依赖安装 + 运行）。
  - 在 `.env.example` 中补充所有配置项注释；验证生产环境配置流程。
  - （可选）编写 GitHub Actions 工作流：提交时自动 build、lint。
- **关联文件**：`docker-compose.yml`、新增 `Dockerfile`、`.env.example`
- **输出物**：一键启动完整环境的 `docker compose up`；容器化部署笔记。

### Task 5.5：LLM 网关层抽象（AI Gateway）
- **学习目标**：理解 AI 网关的定位与职责（统一接入、路由、缓存、限流、降级、可观测、合规），掌握中间件链式架构在 LLM 调用场景的落地方式；能讲清楚「自研轻量网关 vs LiteLLM/Portkey/Cloudflare AI Gateway 等成熟方案」的取舍。
- **背景说明**：当前 `src/agent/llm.ts` 直接耦合具体 Provider 协议，Task 1.2（token 统计）、5.2（模型路由）、5.3（容错监控）的能力分散在各处。本任务的目标是把这些跨切面能力**收口到一个网关层**，使业务代码（Agent、RAG、Planner）不再感知 Provider 差异。
- **项目实践**：
  - **接口抽象**：新建 `src/gateway/` 目录，定义统一接口 `LLMGateway.chat(req): Promise<ChatResponse>` 与流式接口 `chatStream(req)`，业务代码改为依赖该接口而非直连 Provider SDK。
  - **中间件链路**：实现可组合的中间件管道 `request → router → cache → ratelimit → provider adapter → retry → metrics → response`，每一层职责单一、可独立开关。
  - **多 Provider 适配器**：至少实现 2 个 adapter（如 MiniMax + OpenAI 兼容协议），通过配置切换，证明抽象有效。
  - **语义缓存 MVP**：复用阶段3的 embedding 能力，对入参 messages 做向量化，命中阈值（如 cosine ≥ 0.95）直接返回缓存响应；缓存可先用内存 LRU 或 MySQL 落盘。
  - **限流与配额**：基于 user_id / session_id 的滑动窗口限流；预留按 project 维度的预算上限（超额拒绝或降级到轻量模型）。
  - **统一可观测**：在网关入口生成 `trace_id`，串联 router 决策、cache 命中、retry 次数、provider 名、token 用量、总延迟；输出结构化日志。
  - **整合既有任务**：将 Task 5.2 的 router、Task 5.3 的 retry/metrics 重构为网关中间件而非散落在 `llm.ts`。
- **关联文件**：新增 `src/gateway/index.ts`、`src/gateway/middleware/{router,cache,ratelimit,retry,metrics}.ts`、`src/gateway/providers/{minimax,openai}.ts`；重构 `src/agent/llm.ts` 改为薄封装。
- **对照实验**：开关「语义缓存」「智能路由」前后，统计相同测试集的成本（token 消耗）、平均延迟、缓存命中率；写入 benchmark。
- **输出物**：可插拔中间件的网关层；多 Provider 切换 Demo；带缓存/路由前后对比的实验数据；一段能讲清楚「为什么需要网关、自研还是用 LiteLLM」的技术决策记录。

---

## 阶段6：项目实战与面试准备（第9周 ~ 第10周，约2周）

### Task 6.1：端到端测试与性能基准
- **学习目标**：掌握项目收尾的工程标准；能给出诚实的性能指标。
- **项目实践**：
  - 编写端到端测试脚本（调用 `/sessions/:id/chat` 的多种场景），统计：
    - 平均首 token 延迟
    - 平均总响应时间
    - RAG 检索准确率（对比测试集标准答案）
    - 工具调用成功率
  - 将测试结果写入 `docs/benchmark.md`。
- **关联文件**：新增 `scripts/e2e-test.ts`、`docs/benchmark.md`
- **输出物**：可复现的测试脚本；基准数据。

### Task 6.2：README 与架构文档
- **学习目标**：掌握 STAR 法描述项目；README 是面试官的第一印象。
- **项目实践**：
  - 重写项目 README：项目背景、架构图（Mermaid）、核心功能截图/录屏、运行步骤、技术栈、目录结构。
  - 编写 `docs/architecture.md`：从「为什么选 ReAct」「为什么做混合检索」「多 Agent 怎么分工」等角度，用面试问答集中的 STAR 结构写技术决策文档。
- **关联文件**：`README.md`、新增 `docs/architecture.md`
- **输出物**：可直接贴到简历里的项目描述；3 分钟 Demo 脚本。

### Task 6.3：面试问答集与项目映射
- **学习目标**：将 92 道面试题与自己的代码一一对应，做到「问到哪题都能走到代码里讲」。
- **项目实践**：
  - 新建 `docs/interview-mapping.md`：逐题标注「本项目哪段代码能支撑这个回答」。
  - 重点准备 8~10 个「技术深挖」故事：
    - Transformer / Attention（阶段1笔记）
    - Prompt 迭代过程（阶段2评测记录）
    - RAG 架构设计（阶段3实现）
    - Agent 循环与 MCP（阶段4实现）
    - 多 Agent 与工程化（阶段5实现）
    - 线上故障处理（阶段5容错设计）
  - 每周进行 2 次模拟面试（可录音自问自答）。
- **关联文件**：新增 `docs/interview-mapping.md`
- **输出物**：面试题-代码映射表；至少 4 个完整 STAR 故事。

---

## 总体时间线

| 周次 | 阶段 | 核心交付物 |
|------|------|-----------|
| 第1周上 | 阶段1 补强 | 流式接口 + Token 统计 |
| 第1周下~第2周 | 阶段2 Prompt Engineering | 提示词版本化 + Few-shot/CoT + 评测框架 |
| 第3周~第4周中 | 阶段3 RAG(Task 3.1~3.5) | 完整 RAG 流水线(切分/向量/检索/混合/溯源) |
| 第4周末~第5周初 | **整合阶段(Task 整合-1/-2)** | **LangGraph 主线切换 + Run-as-Resource 完整版**(主动取消/事件 seq 回放/三态 abort/多订阅者/持久化 Checkpointer) |
| 第5周 | 阶段3 续(Task 3.6/3.7) | 本地 embedding + 联网搜索 |
| 第5周末~第6周中 | 阶段4 Agent 核心 | ReAct 增强 + Plan 模式 + 记忆分层 + MCP+Skills(原 Task 4.5 已并入整合阶段、Task 4.6 取消) |
| 第7周~第8周 | 阶段5 工程化 | 多 Agent + 路由/降级 + 容错监控 + 容器化 + LLM 网关层 |
| 第9周~第10周 | 阶段6 面试准备 | 测试基准 + README/架构文档 + 面试映射 |

---

## 关键设计决策说明

1. **为什么从阶段2开始，阶段1只补 3~4 天？**
   项目已有 LLM 调用和 API 基础，阶段1的核心概念（Transformer、Token、参数）可通过「流式输出 + Token 统计」这两个小任务快速覆盖，不需要单独花整周时间。

2. **为什么 RAG 选 Chroma 而不是 MySQL JSON 列 或 Milvus/Pinecone？**
   三档方案的取舍：① **MySQL JSON 列**——0 依赖,但全表扫描无索引、不能讲 RAG 行业标准接口、性能在几千 chunk 就开始抖,**学完玩具感太强**;② **Milvus / Pinecone**——生产级别但运维 / 成本太重,跟我们"几百个目的地"的数据量完全不匹配,反而会让"为什么选 Milvus"无法自圆其说;③ **Chroma**——轻量(docker 一键起)、接口贴合 LangChain/LlamaIndex 生态、内置 HNSW 索引、能讲清楚选型理由——是学习项目里最贴近真实生产架构的甜蜜点。**接口仍然抽象成 `VectorStore`**(`src/rag/vectorStore.ts`),后续真有"亿级向量"需求,只换实现不动业务代码。这也对应面试问答集"技术选型不是越重越好,要匹配数据规模 + 团队成本"的思路。

3. **为什么阶段4要同时保留 ReAct 和 Plan-and-Execute？**
   面试问答集 Q1 就是「为什么选择 ReAct 而不是 Plan-and-Execute」。项目中同时实现两种模式并支持切换，既能深入理解两者差异，又能在面试中用实际数据（步骤数、耗时、完成率）支撑回答。

4. **为什么阶段4要把 Run 持久化（4.5）和记忆分层（4.3）绑在一起？**
   两者本质都在回答"什么状态需要跨连接、跨会话存活"——4.5 处理"事件流水"（episodic 记忆），4.3 处理"语义摘要"（semantic 记忆），共用 `agent_runs` 持久层、共用 `RUN_FINISHED` 触发时机。技术上 4.5 是 4.3 的底座（摘要生成需要订阅 Run 完成事件）；面试上这对组合能直接对应八股 05 §短期/长期记忆，是非常完整的故事点。同时 4.5 还纠正了阶段1"客户端断开即终止 Run"的 Chat App 心智，把项目从"对话工具"提升为"Run-as-Resource"的 Agent 系统——这是阶段3~5 工程化故事的关键转折。

5. **为什么阶段5要单独抽出 LLM 网关层（Task 5.5）？**
   网关层是大厂 LLM 应用必备的工程化能力，能在一处集中处理「统一接入、路由、缓存、限流、降级、可观测、合规」等跨切面问题。Task 1.2、5.2、5.3 的能力如果散落在 `llm.ts` 里，面试时只能讲点状能力；而把它们收口为一个网关，可以同时覆盖「架构分层」「中间件模式」「成本治理」「可观测性」「降级容错」5 个常考点，是性价比最高的工程化故事点。同时本任务也对应面试中「自研 vs LiteLLM/Portkey 等成熟方案如何取舍」的高频问题。

6. **为什么把 LangGraph 主线切换 + Run-as-Resource 完整版都拆成"整合阶段"?**
   三个原因:① **范围边界**——这两件事是横跨"阶段3 已建的工具/RAG/sanitize/sources/prompts"和"阶段4 即将做的 ReAct 增强/MCP"的横切面改动,塞任一阶段都会稀释叙事;独立成"整合阶段"让两侧都干净。② **接框架降本**——整合-2 的会话续流需求(切走 Run 不停、主动取消、事件回放等)如果靠手写实现就是 Task 4.5 的全部复杂度;LangGraph 的 `thread_id` + 持久化 Checkpointer 是现成的,**接框架的成本远低于手写**——所以决定整合-1 先切 LangGraph 主线(Task 4.6 取消),整合-2 在 LangGraph 基础上一次性做 Run-as-Resource 完整版。③ **一次到位 vs 两步走**——最初规划"整合-2 轻量版 + Task 4.5 完整版"两步;后改为整合-2 直接做完整版(Task 4.5 整体并入),理由是轻量版基础设施(runRegistry、DB 表、handler 改造)和完整版重叠 70%,拆两步会产生重复工作 + 接口反复调整。**手写 `runAgentStream` 直接删除**(不保留作历史参考——STAR 故事写在 `docs/03-开发笔记/note-04` 即可,源码留档反而成腐烂死代码),"手写 vs LangGraph 对比" STAR 故事在整合-1 输出物里讲——这是真实工程常见决策:**发现某能力靠手写要 2 周、接框架只要 2 天,就该接框架**;**发现"轻量版 + 完整版两步走"会重复造轮子,就一次做完整版**。阶段4 因此简化为 4.1/4.2/4.3/4.4 四个任务。

7. **为什么 Task 3.6 / 3.7 把 embedding 本地化 + 加联网搜索?**
   Task 3.1 落地时 MiniMax 账号无 embedding 权限,临时用 `deterministic`(字符 n-gram)兜底,流程通但**无真语义**(`exp-04` 实测 q2 "辣"无法关联"火锅")。本地 embedding 走 Transformers.js + Xenova/bge-small-zh-v1.5(ONNX 量化版,中文专项,Node 进程内运行,零 docker 服务),跟现有 Embedder 抽象 1 行 `.env` 切换。联网搜索(Tavily)解决"知识库只有 3 个目的地"的玩具感——这两个任务一起让"RAG 全链路"从演示态升级为真实可演示的"能搜任意目的地 + 有真语义"系统,**为阶段4 MCP/Skills 提供更丰富的工具池**。

8. **为什么 Task 4.4 把 MCP 和 Agent Skills 绑在一起?**
   单独看 MCP 是协议升级(本地直调 → 跨进程调用),收益主要在工程层(工具复用、跨语言);单独看 Skills 是抽象升级(散乱 tool → 组合能力包),收益主要在模型层(决策复杂度降低)。**两者绑在一起讲**才能完整对应"工具(Tool)→ 工具集(Server)→ 技能(Skill)"三层抽象的演进——这是 2025 年 Agent 工程的最重要趋势,且能直接对接 Anthropic Agent Skills / Claude Code skills 等业界规范。

9. **每个阶段的「输出物」是什么？**
   每阶段至少产出：① 可运行的代码 + ② 笔记/实验记录 + ③ 一个能对外演示的效果。阶段6 产出简历条目和面试故事。
