{
  "name": "guide-plan-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "setup": "docker compose up -d && tsx scripts/migrate.ts && tsx scripts/seed.ts",
    "web:dev": "cd web && npm run dev",
    "dev:all": "docker compose up -d && tsx scripts/migrate.ts && tsx scripts/seed.ts && concurrently \"tsx watch src/index.ts\" \"cd web && npm run dev\""
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "dotenv": "^17.4.2",
    "fastify": "^5.2.0",
    "mysql2": "^3.11.5",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "concurrently": "^9.2.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "repository": "git@github.com:1strive/guide-plan-agent.git"
}
