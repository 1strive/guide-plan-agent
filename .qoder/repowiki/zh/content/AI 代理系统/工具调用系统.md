# 工具调用系统

<cite>
**本文档引用的文件**
- [src/agent/tools.ts](file://src/agent/tools.ts)
- [src/agent/llm.ts](file://src/agent/llm.ts)
- [src/agent/prompts.ts](file://src/agent/prompts.ts)
- [src/db/destinationRepo.ts](file://src/db/destinationRepo.ts)
- [src/db/ragRepo.ts](file://src/db/ragRepo.ts)
- [src/db/pool.ts](file://src/db/pool.ts)
- [src/rag/embed.ts](file://src/rag/embed.ts)
- [src/rag/similarity.ts](file://src/rag/similarity.ts)
- [src/rag/ingest.ts](file://src/rag/ingest.ts)
- [src/config.ts](file://src/config.ts)
- [src/index.ts](file://src/index.ts)
- [src/db/migrations/001_init.sql](file://src/db/migrations/001_init.sql)
- [package.json](file://package.json)
</cite>

## 目录
1. [简介](#简介)
2. [项目结构](#项目结构)
3. [核心组件](#核心组件)
4. [架构概览](#架构概览)
5. [详细组件分析](#详细组件分析)
6. [依赖关系分析](#依赖关系分析)
7. [性能考虑](#性能考虑)
8. [故障排除指南](#故障排除指南)
9. [结论](#结论)
10. [附录](#附录)

## 简介

Guide-Plan-Agent 是一个基于大语言模型的智能旅游规划助手，其核心功能是通过工具调用系统实现与外部数据源的交互。该系统采用函数式工具设计模式，支持目的地查询、详情获取和语义检索等功能，能够根据用户需求自动选择合适的工具并执行相应的数据操作。

系统的核心设计理念是"工具即服务"，每个工具都是独立的功能模块，具有明确的输入输出规范和错误处理机制。通过标准化的工具接口，系统实现了灵活的工具组合和链式调用，为用户提供智能化的旅游规划服务。

## 项目结构

项目采用模块化的目录结构，按照功能域进行组织：

```mermaid
graph TB
subgraph "应用层"
Index[src/index.ts]
Prompts[src/agent/prompts.ts]
end
subgraph "代理层"
LLM[src/agent/llm.ts]
Tools[src/agent/tools.ts]
end
subgraph "数据访问层"
Pool[src/db/pool.ts]
DestRepo[src/db/destinationRepo.ts]
RagRepo[src/db/ragRepo.ts]
end
subgraph "RAG引擎"
Embed[src/rag/embed.ts]
Similarity[src/rag/similarity.ts]
Ingest[src/rag/ingest.ts]
end
subgraph "配置层"
Config[src/config.ts]
Schema[src/db/migrations/001_init.sql]
end
Index --> LLM
LLM --> Tools
Tools --> DestRepo
Tools --> RagRepo
RagRepo --> Embed
RagRepo --> Similarity
DestRepo --> Pool
RagRepo --> Pool
LLM --> Config
Tools --> Config
Embed --> Config
```

**图表来源**
- [src/index.ts:1-77](file://src/index.ts#L1-L77)
- [src/agent/llm.ts:1-114](file://src/agent/llm.ts#L1-L114)
- [src/agent/tools.ts:1-195](file://src/agent/tools.ts#L1-L195)

**章节来源**
- [src/index.ts:1-77](file://src/index.ts#L1-L77)
- [src/agent/llm.ts:1-114](file://src/agent/llm.ts#L1-L114)
- [src/agent/tools.ts:1-195](file://src/agent/tools.ts#L1-L195)

## 核心组件

### 工具定义系统

工具定义系统采用声明式设计，通过统一的工具定义数组来管理所有可用工具。每个工具都包含完整的元数据信息，包括名称、描述和参数规范。

```mermaid
classDiagram
class ToolDefinition {
+string type
+string name
+string description
+Object parameters
+string[] required
}
class ToolRunResult {
+string text
+number[] referencedDestinationIds
}
class ToolArgs {
<<interface>>
}
class SearchDestinationsArgs {
+string query
+string region
+number limit
}
class SemanticSearchArgs {
+string query
+number top_k
+string region
}
class GetDestinationDetailArgs {
+number destination_id
}
ToolDefinition --> ToolRunResult : "produces"
ToolArgs <|-- SearchDestinationsArgs
ToolArgs <|-- SemanticSearchArgs
ToolArgs <|-- GetDestinationDetailArgs
```

**图表来源**
- [src/agent/tools.ts:15-65](file://src/agent/tools.ts#L15-L65)
- [src/agent/tools.ts:10-13](file://src/agent/tools.ts#L10-L13)
- [src/agent/tools.ts:71-77](file://src/agent/tools.ts#L71-L77)

### 参数解析器

参数解析器负责将原始的 JSON 字符串转换为强类型的工具参数对象，同时进行必要的类型转换和验证。

```mermaid
flowchart TD
Start([开始解析]) --> ParseJSON["解析JSON字符串"]
ParseJSON --> CheckName{"检查工具名称"}
CheckName --> |search_destinations| ParseSearch["解析搜索参数"]
CheckName --> |semantic_search_travel| ParseSemantic["解析语义搜索参数"]
CheckName --> |get_destination_detail| ParseDetail["解析详情参数"]
CheckName --> |其他| ThrowError["抛出未知工具错误"]
ParseSearch --> ConvertTypes["转换数据类型<br/>- query: string<br/>- region: string<br/>- limit: number"]
ParseSemantic --> ConvertTypes
ParseDetail --> ValidateId["验证destination_id<br/>- 转换为数字<br/>- 检查有效性"]
ConvertTypes --> ReturnArgs["返回工具参数对象"]
ValidateId --> ReturnArgs
ThrowError --> End([结束])
ReturnArgs --> End
```

**图表来源**
- [src/agent/tools.ts:79-112](file://src/agent/tools.ts#L79-L112)

**章节来源**
- [src/agent/tools.ts:15-112](file://src/agent/tools.ts#L15-L112)

## 架构概览

系统采用分层架构设计，从上到下分为表现层、代理层、工具层和数据访问层：

```mermaid
graph TB
subgraph "表现层"
API[HTTP API]
WebUI[Web界面]
end
subgraph "代理层"
Agent[LLM代理]
ToolManager[工具管理器]
end
subgraph "工具层"
SearchTool[搜索工具]
DetailTool[详情工具]
SemanticTool[语义搜索工具]
end
subgraph "数据访问层"
MySQL[MySQL数据库]
RAG[RAG向量存储]
end
subgraph "配置层"
Env[环境配置]
Models[模型配置]
end
API --> Agent
WebUI --> Agent
Agent --> ToolManager
ToolManager --> SearchTool
ToolManager --> DetailTool
ToolManager --> SemanticTool
SearchTool --> MySQL
DetailTool --> MySQL
SemanticTool --> MySQL
SemanticTool --> RAG
Agent --> Env
ToolManager --> Env
SearchTool --> Models
SemanticTool --> Models
```

**图表来源**
- [src/index.ts:35-68](file://src/index.ts#L35-L68)
- [src/agent/llm.ts:49-114](file://src/agent/llm.ts#L49-L114)
- [src/agent/tools.ts:67-195](file://src/agent/tools.ts#L67-L195)

## 详细组件分析

### 工具执行引擎

工具执行引擎是整个系统的核心，负责协调工具的调用、参数解析和结果处理。

```mermaid
sequenceDiagram
participant Client as 客户端
participant Agent as LLM代理
participant Tool as 工具执行器
participant Repo as 数据仓库
participant DB as MySQL数据库
Client->>Agent : 用户消息
Agent->>Agent : 生成工具调用请求
Agent->>Tool : 执行工具调用
Tool->>Tool : 解析参数
Tool->>Repo : 查询数据
Repo->>DB : 执行SQL查询
DB-->>Repo : 返回查询结果
Repo-->>Tool : 返回处理后的数据
Tool-->>Agent : 工具执行结果
Agent-->>Client : 最终响应
Note over Agent,Tool : 支持多轮工具调用和错误处理
```

**图表来源**
- [src/agent/llm.ts:49-114](file://src/agent/llm.ts#L49-L114)
- [src/agent/tools.ts:114-195](file://src/agent/tools.ts#L114-L195)

#### 搜索工具实现

搜索工具支持结构化的目的地查询，具备关键词匹配和区域筛选功能：

```mermaid
flowchart TD
Input[接收搜索参数] --> Validate[验证参数]
Validate --> BuildSQL[构建SQL查询]
BuildSQL --> AddRegionFilter{"是否指定区域?"}
AddRegionFilter --> |是| RegionFilter[添加区域过滤条件]
AddRegionFilter --> |否| NoRegion[无区域过滤]
RegionFilter --> Execute[执行查询]
NoRegion --> Execute
Execute --> LimitResults[限制结果数量]
LimitResults --> ExtractIDs[提取目的地ID]
ExtractIDs --> ReturnResult[返回结果]
```

**图表来源**
- [src/agent/tools.ts:121-141](file://src/agent/tools.ts#L121-L141)
- [src/db/destinationRepo.ts:20-45](file://src/db/destinationRepo.ts#L20-L45)

#### 详情获取工具实现

详情获取工具提供目的地的完整信息，包括结构化详情和分类特征：

```mermaid
sequenceDiagram
participant Tool as 工具执行器
participant DestRepo as 目的地仓库
participant FeatRepo as 特征仓库
Tool->>DestRepo : 获取目的地详情
DestRepo-->>Tool : 返回目的地信息
alt 目的地存在
Tool->>FeatRepo : 获取分类特征
FeatRepo-->>Tool : 返回特征列表
Tool->>Tool : 分组处理特征
Tool->>Tool : 组装响应数据
Tool-->>Tool : 返回详细信息
else 目的地不存在
Tool->>Tool : 返回错误信息
Tool-->>Tool : 返回空引用ID
end
```

**图表来源**
- [src/agent/tools.ts:162-192](file://src/agent/tools.ts#L162-L192)
- [src/db/destinationRepo.ts:47-85](file://src/db/destinationRepo.ts#L47-L85)

#### 语义搜索工具实现

语义搜索工具基于向量相似度进行智能检索，支持自然语言查询和区域预筛选：

```mermaid
flowchart TD
Query[接收查询参数] --> ValidateRegion{"是否指定区域?"}
ValidateRegion --> |是| GetDestIDs[获取区域相关目的地ID]
ValidateRegion --> |否| SkipRegion[跳过区域筛选]
GetDestIDs --> CheckEmpty{"是否有匹配目的地?"}
CheckEmpty --> |否| ReturnEmpty[返回空结果]
CheckEmpty --> |是| LoadChunks[加载候选片段]
SkipRegion --> LoadChunks
LoadChunks --> EmbedQuery[生成查询向量]
EmbedQuery --> CalculateSimilarity[计算余弦相似度]
CalculateSimilarity --> TopK[选择Top-K结果]
TopK --> ExtractDestIDs[提取目的地ID]
ExtractDestIDs --> ReturnResult[返回语义搜索结果]
```

**图表来源**
- [src/agent/tools.ts:142-161](file://src/agent/tools.ts#L142-L161)
- [src/db/ragRepo.ts:97-142](file://src/db/ragRepo.ts#L97-L142)

**章节来源**
- [src/agent/tools.ts:114-195](file://src/agent/tools.ts#L114-L195)
- [src/db/destinationRepo.ts:20-100](file://src/db/destinationRepo.ts#L20-L100)
- [src/db/ragRepo.ts:97-142](file://src/db/ragRepo.ts#L97-L142)

### 上下文传递机制

系统实现了完整的上下文传递机制，确保工具调用过程中的状态保持和信息共享：

```mermaid
classDiagram
class ChatMessage {
+string role
+string content
+ToolCall[] tool_calls
+string tool_call_id
}
class ToolCall {
+string id
+string type
+FunctionCall function
}
class FunctionCall {
+string name
+string arguments
}
class ToolRunResult {
+string text
+number[] referencedDestinationIds
}
ChatMessage --> ToolCall : "包含"
ToolCall --> FunctionCall : "包含"
ToolRunResult --> ChatMessage : "作为响应"
```

**图表来源**
- [src/agent/llm.ts:5-18](file://src/agent/llm.ts#L5-L18)

**章节来源**
- [src/agent/llm.ts:49-114](file://src/agent/llm.ts#L49-L114)

### 结果聚合与引用管理

系统实现了智能的结果聚合和引用目的地ID收集机制：

```mermaid
flowchart TD
Start[开始工具调用循环] --> CheckToolCalls{是否有工具调用?}
CheckToolCalls --> |是| ProcessToolCalls[处理工具调用]
CheckToolCalls --> |否| ReturnFinal[返回最终结果]
ProcessToolCalls --> ExecuteTool[执行单个工具]
ExecuteTool --> CollectRefs[收集引用ID]
CollectRefs --> AddToHistory[添加到对话历史]
AddToHistory --> NextTool{还有更多工具?}
NextTool --> |是| ProcessToolCalls
NextTool --> |否| ContinueLoop[继续下一轮]
ContinueLoop --> CheckMaxRounds{超过最大轮次?}
CheckMaxRounds --> |是| ThrowError[抛出超时错误]
CheckMaxRounds --> |否| CheckToolCalls
ReturnFinal --> ExtractUnique[去重引用ID]
ExtractUnique --> ReturnResponse[返回最终响应]
```

**图表来源**
- [src/agent/llm.ts:59-113](file://src/agent/llm.ts#L59-L113)

**章节来源**
- [src/agent/llm.ts:59-113](file://src/agent/llm.ts#L59-L113)

## 依赖关系分析

系统采用松耦合的设计原则，各组件之间的依赖关系清晰明确：

```mermaid
graph TB
subgraph "外部依赖"
Fastify[Fastify Web框架]
MySQL2[MySQL2驱动]
Zod[Zod类型验证]
OpenAI[OpenAI API]
end
subgraph "内部模块"
Index[index.ts]
LLM[llm.ts]
Tools[tools.ts]
Config[config.ts]
Pool[pool.ts]
DestRepo[destinationRepo.ts]
RagRepo[ragRepo.ts]
Embed[embed.ts]
Similarity[similarity.ts]
Ingest[ingest.ts]
end
Index --> Fastify
Index --> LLM
LLM --> Tools
Tools --> DestRepo
Tools --> RagRepo
Tools --> Config
DestRepo --> Pool
RagRepo --> Pool
RagRepo --> Embed
RagRepo --> Similarity
Embed --> Config
Config --> Zod
Index --> MySQL2
LLM --> OpenAI
```

**图表来源**
- [package.json:18-30](file://package.json#L18-L30)
- [src/index.ts:1-77](file://src/index.ts#L1-L77)
- [src/agent/llm.ts:1-114](file://src/agent/llm.ts#L1-L114)
- [src/agent/tools.ts:1-195](file://src/agent/tools.ts#L1-L195)

**章节来源**
- [package.json:18-30](file://package.json#L18-L30)
- [src/config.ts:1-46](file://src/config.ts#L1-L46)

## 性能考虑

### 查询优化策略

系统在多个层面实现了性能优化：

1. **数据库查询优化**
   - 使用适当的索引策略（主键、外键、复合索引）
   - 实现查询参数化，防止SQL注入
   - 限制查询结果集大小，避免内存溢出

2. **缓存机制**
   - 向量嵌入结果缓存
   - 查询结果缓存
   - 配置参数缓存

3. **并发控制**
   - 连接池管理
   - 异步查询执行
   - 超时控制

### 内存管理

系统采用流式处理和分页机制来控制内存使用：

```mermaid
flowchart TD
Data[大量数据] --> Stream[流式处理]
Stream --> Batch[批量处理]
Batch --> Process[处理单批数据]
Process --> MemoryCheck{内存使用检查}
MemoryCheck --> |正常| NextBatch[处理下一批]
MemoryCheck --> |过高| Cleanup[清理缓存]
Cleanup --> NextBatch
NextBatch --> Process
```

## 故障排除指南

### 常见问题诊断

1. **工具调用失败**
   - 检查工具名称是否正确
   - 验证参数格式和类型
   - 查看数据库连接状态

2. **查询性能问题**
   - 分析SQL执行计划
   - 检查索引使用情况
   - 监控数据库负载

3. **内存泄漏**
   - 检查异步操作的清理
   - 验证事件监听器的移除
   - 监控对象引用关系

### 错误处理机制

系统实现了多层次的错误处理：

```mermaid
flowchart TD
Error[发生错误] --> LogError[记录错误日志]
LogError --> CheckType{检查错误类型}
CheckType --> |工具参数错误| ValidateParams[参数验证]
CheckType --> |数据库连接错误| Reconnect[重新连接]
CheckType --> |网络请求错误| Retry[重试机制]
CheckType --> |业务逻辑错误| Fallback[回退策略]
ValidateParams --> ReturnError[返回错误信息]
Reconnect --> ReturnError
Retry --> ReturnError
Fallback --> ReturnPartial[返回部分结果]
ReturnError --> Cleanup[清理资源]
ReturnPartial --> Cleanup
Cleanup --> End[结束处理]
```

**章节来源**
- [src/agent/llm.ts:95-101](file://src/agent/llm.ts#L95-L101)
- [src/agent/tools.ts:103-112](file://src/agent/tools.ts#L103-L112)

## 结论

Guide-Plan-Agent 的工具调用系统展现了现代AI应用的优秀架构设计。通过模块化的工具定义、严格的参数验证和智能的上下文管理，系统实现了高度可扩展和可维护的工具调用机制。

系统的主要优势包括：
- **模块化设计**：每个工具都是独立的功能单元
- **类型安全**：完整的TypeScript类型定义
- **错误处理**：完善的异常处理和恢复机制
- **性能优化**：多层缓存和查询优化
- **扩展性**：易于添加新工具和自定义业务逻辑

## 附录

### 工具扩展指南

#### 添加新工具的步骤

1. **定义工具元数据**
   ```typescript
   // 在工具定义数组中添加新工具
   const newToolDefinition = {
     type: 'function',
     function: {
       name: 'your_tool_name',
       description: '工具功能描述',
       parameters: {
         type: 'object',
         properties: {
           // 定义参数
         },
         required: [] // 必需参数列表
       }
     }
   }
   ```

2. **实现工具逻辑**
   ```typescript
   // 在runTool函数中添加新工具分支
   if (parsed.name === 'your_tool_name') {
     // 实现工具逻辑
     return {
       text: JSON.stringify(result),
       referencedDestinationIds: ids
     }
   }
   ```

3. **更新类型定义**
   ```typescript
   // 更新ToolArgs联合类型
   type ToolArgs =
     // ... 其他工具类型
     | { name: 'your_tool_name'; args: YourToolArgs }
   ```

#### 自定义业务逻辑最佳实践

1. **参数验证**
   - 始终验证输入参数的有效性
   - 提供合理的默认值
   - 实施参数范围检查

2. **错误处理**
   - 使用具体的错误类型
   - 记录详细的错误上下文
   - 提供有意义的错误消息

3. **性能优化**
   - 实施适当的缓存策略
   - 优化数据库查询
   - 控制内存使用

4. **测试策略**
   - 编写单元测试
   - 实施集成测试
   - 进行性能测试

### 调试技巧

1. **日志记录**
   - 记录工具调用的输入输出
   - 跟踪数据库查询执行时间
   - 监控API调用响应

2. **监控指标**
   - 工具调用成功率
   - 平均响应时间
   - 错误率统计

3. **性能分析**
   - 使用性能分析工具
   - 监控内存使用情况
   - 分析数据库查询性能