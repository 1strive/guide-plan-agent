# ── 高德地图 AMap JS API（前端导航地图渲染）──────────────────────
# 需要「Web端(JS API)」类型的 Key，与后端 MCP 用的 Web服务 Key 是不同类型
# 申请地址：https://console.amap.com/dev/key/app → 创建应用 → 添加 Key → 服务平台选择「Web端(JS API)」
# VITE_AMAP_SECURITY_CODE 为 JS API 2.0 安全密钥（在同一个 Key 管理页获取）
# 未配置时前端会显示占位提示「未配置高德 JS Key」，不会崩溃
VITE_AMAP_JS_KEY=
VITE_AMAP_SECURITY_CODE=
